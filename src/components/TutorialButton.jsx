import {
    IconButton
  } from '@chakra-ui/react';
import { AiOutlineQuestionCircle } from 'react-icons/ai';

export const TutorialButton = () => {
    return (
        <IconButton icon={<AiOutlineQuestionCircle />} />
    );
};